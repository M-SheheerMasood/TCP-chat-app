Python Network Communications.
This is about using a custom chat application that uses a client-server setup and allows for file transfer and messages via TCP. 
I only worked on "client.py" and "server.py"; the rest is supporting code and tests provided by the course staff. 
To run: "python3 TestChatApp.py"
